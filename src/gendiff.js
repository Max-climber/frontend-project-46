// src/gendiff.js
const gendiff = (filepath1, filepath2, format) => {
    console.log('filepath1:', filepath1); // Временно, для проверки
    console.log('filepath2:', filepath2); // Временно, для проверки
    console.log('format:', format); // Временно, для проверки
    return 'Diff result'; // Временно, для проверки
  };
  
  export default gendiff;
  